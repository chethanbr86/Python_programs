from functools import reduce

num = 35

num_list = list(map(int, str(num)))
print(num_list)

#for i in range(num_list+1):
fact_list1 = reduce(lambda x, y: x*y, range(1, num_list[0]+1))
fact_list2 = reduce(lambda x, y: x*y, range(1, num_list[1]+1)) 
print(fact_list1+fact_list2)


