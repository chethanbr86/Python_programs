from functools import reduce

num = 35

num_list = list(map(int, str(num)))
print(num_list)

#factorial of each number in list
fact1 = reduce(lambda x, y: x*y, range(1, num_list[0]+1))
fact2 = reduce(lambda x, y: x*y, range(1, num_list[1]+1)) 
print(fact1+fact2)



