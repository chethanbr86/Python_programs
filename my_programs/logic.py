# while total_exchange >= numExchange:    
#             # new_bottles = total_exchange // numExchange
#             total_full = total_full + (total_exchange // numExchange)
#             total_exchange = (total_exchange // numExchange) + (total_exchange % numExchange)